{
  "name": "biolab-leads-api",
  "version": "0.1.0",
  "private": true,
  "description": "Public query + enrichment API for biolab-leads (Lambda)",
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "@aws-sdk/client-dynamodb": "^3.600.0",
    "@aws-sdk/client-s3": "^3.600.0",
    "@aws-sdk/lib-dynamodb": "^3.600.0"
  }
}
